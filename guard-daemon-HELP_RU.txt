═══════════════════════════════════════════════════════════════════
  guard-daemon — Справка (RU)
═══════════════════════════════════════════════════════════════════

НАЗНАЧЕНИЕ
───────────────────────────────────────────────────────────────────
Высокопроизводительный Go-демон для автоматического спасения токенов
со скомпрометированного кошелька (EOA) с использованием **EIP-7702**.

Мониторит ВСЕ входящие ERC-20 транзакции (включая неизвестные
airdrops) и мгновенно пересылает их на безопасный адрес через
атомарные транзакции.

**Главное:** одна транзакция = делегация + sweep. Никакого окна
для перехвата ботом.


ПРОБЛЕМА И РЕШЕНИЕ
───────────────────────────────────────────────────────────────────
Проблема (традиционный подход):
  tx1: сделать клейм токена
  tx2: переправить на безопасный адрес
  ↑ Между tx1 и tx2 (6-12 сек) бот мошенника перехватывает токены.

Решение (EIP-7702):
  Одна атомарная транзакция:
    1. SetCode авторизация (делегирование на RescuerV2)
    2. Вызов sweepAll([адрес_токена])
    
  ↑ Всё в одном блоке. Нет окна для атаки.


БЫСТРЫЙ СТАРТ
───────────────────────────────────────────────────────────────────
1. Установи Go 1.22+
   https://golang.org/dl

2. Скачай или склонируй:
   git clone https://github.com/Serge693/guard-daemon.git
   cd guard-daemon

3. Собери:
   go mod tidy
   go build -o guard-daemon.exe main.go
   (Linux/Mac: go build -o guard-daemon main.go)

4. Настрой .env:
   cp .env.example .env
   
   Отредактируй:
     SOURCE_PRIVATE_KEY=0x... (взломанный кошелёк)
     SPONSOR_PRIVATE_KEY=0x... (платит газ)
     DESTINATION_ADDRESS=0x... (безопасный адрес)

5. Запусти:
   ./guard-daemon.exe

Ожидаемый вывод:
   08:22:15.874 [Base     ] ⚡ OFC Transfer! tx=0xe42a...
   08:22:16.637 [Base     ] -> renewAndSweep(OFC) tx=0xea5...
   08:22:17.741 [Base     ] ✓ Swept OFC → destination


НАСТРОЙКА .env
───────────────────────────────────────────────────────────────────
Обязательные переменные:

  SOURCE_PRIVATE_KEY
    Приватный ключ скомпрометированного кошелька.
    Используется ТОЛЬКО для подписи SetCode авторизации локально.
    Не требует ETH для газа (платит спонсор).
    Пример: 0xabc123...

  SPONSOR_PRIVATE_KEY
    Приватный ключ кошелька-спонсора.
    Платит за все транзакции делегирования и sweep.
    Требует ~0.01 ETH на каждую сеть.
    Пример: 0xdef456...

  DESTINATION_ADDRESS
    Безопасный адрес, куда переводятся спасённые токены.
    Может быть холодный кошелёк, мультисиг, аппаратный кошелёк.
    Пример: 0x1b3892...

───────────────────────────────────────────────────────────────────
Конфигурация сетей:

  RESCUER_BASE
    Адрес контракта RescuerV2 на Base (заполняется при деплое).
    По умолчанию: 0x63635ac6b448965d08c03e9dab3066bebf050c09

  RESCUER_ETHEREUM, RESCUER_ARBITRUM, RESCUER_OPTIMISM, и т.д.
    Адреса для остальных сетей.

  RPC_URL_BASE, RPC_URL_ETHEREUM, и т.д.
    Пользовательские RPC-эндпоинты (Alchemy, Infura и т.д.).
    Если не задано — используются публичные (могут быть медленнее).

───────────────────────────────────────────────────────────────────
Опциональные параметры:

  RESCUE_TOKENS=0xAddr1,0xAddr2
    Если задано — слушает ТОЛЬКО эти адреса токенов.
    Если пусто — слушает ВСЕ входящие Transfer.
    (Рекомендуется пусто для ловли неизвестных airdrops)

  SPONSOR_MIN_BALANCE=0.01
    Минимальный баланс спонсора перед предупреждением.
    По умолчанию: 0.005 ETH.


ПОДДЕРЖИВАЕМЫЕ СЕТИ
───────────────────────────────────────────────────────────────────
✅ Base (8453)      — основная сеть, низкий газ
✅ Ethereum (1)     — основная сеть
✅ Arbitrum (42161) — Layer 2, средний газ
✅ Optimism (10)    — Layer 2, средний газ
✅ Polygon (137)    — сайдчейн, низкий газ
✅ BNB (56)         — сайдчейн, средний газ
✅ Ink (57073)      — Layer 3, низкий газ
✅ Linea (59144)    — ZK Rollup, средний газ
✅ Scroll (534352)  — ZK Rollup, средний газ
✅ Metis (1088)     — сайдчейн, средний газ

zkSync Era удалён окончательно — секвенсер сети отклоняет EIP-7702
Type-4 транзакции на уровне протокола ("transaction type is not
supported"). Это не баг демона и не проблема RPC — делегация там
принципиально невозможна.

Soneium, 0G, DACC, X1ecochain временно закомментированы в main.go —
либо ещё не задеплоены, либо (Soneium) делегация стабильно не
проходит на практике, несмотря на то что сеть заявлена как
поддерживающая EIP-7702 в документации. Раскомментируй и
задеплой заново, если появится конкретная необходимость.

Для каждой сети требуется:
  - Задеплоенный RescuerV2 (адрес в .env)
  - ~0.01 ETH на спонсоре для газа

Демон при старте на каждой сети проверяет: совпадает ли chainId
у RPC с ожидаемым, и совпадает ли on-chain destination() контракта
с DESTINATION_ADDRESS в .env. Если что-то не совпадает — демон
откажется запускаться на этой сети вместо того, чтобы продолжить
работу с неправильной конфигурацией.


КАК РАБОТАЕТ ДЕМОН
───────────────────────────────────────────────────────────────────
Основной цикл (на каждой сети):

  1. Слушает Transfer-события на блокчейне
     (адрес получателя = SOURCE_ADDRESS)

  2. При получении события:
     a. Определяет токен (известный или запрашивает метаданные)
     b. Проверяет баланс (> 0?)
     c. Запускает renewAndSweep() — атомарная транзакция

  3. renewAndSweep() (одна EIP-7702 транзакция):
     a. SOURCE подписывает SetCode авторизацию (локально, без газа)
     b. SPONSOR собирает tx с авторизацией + вызовом sweepAll()
     c. Отправляет в сеть

  4. RescuerV2 (контракт):
     a. Получает вызов с делегированным SOURCE EOA
     b. Проверяет _verifyDelegation() (делегация на нас?)
     c. Если ОК: переводит весь баланс токена на DESTINATION
     d. Если FAIL: откатывается (токены остаются в безопасности)

  5. Результат:
     ✓ Если успех → токены на DESTINATION
     ✗ Если ошибка → будет повторная попытка автоматически (см. ниже)

Периодическая проверка (каждые ~12 секунд в WebSocket-режиме):
  - Проверяет активна ли делегация
  - Если нарушена: восстанавливает через renewDelegation()
  - Свипирует малое количество нативного газа если много
  - Перепроверяет баланс каждого известного токена на источнике и
    ПОВТОРЯЕТ sweep, если баланс всё ещё положительный — это
    покрывает случай, когда предыдущий renewAndSweep() откатился
    (например, из-за гонки за nonce или временной ошибки RPC), а
    токены остались на месте. Без этого токены, пережившие неудачную
    попытку, просто лежали бы до следующего внешнего Transfer-события,
    которое может никогда не прийти.


ЧТО ДЕМОН ДЕЛАЕТ ХОРОШО
───────────────────────────────────────────────────────────────────
✅ Атомарные транзакции (без окна для перехвата)
✅ Проверка делегации (_verifyDelegation в контракте)
✅ Без кастодии (токены сразу на destination, не в хранилище)
✅ Автоматическое восстановление делегации
✅ Поддержка неизвестных токенов (on-chain lookup метаданных)
✅ Работает 24/7 в фоне


ЧТО ДЕМОН НЕ ДЕЛАЕТ
───────────────────────────────────────────────────────────────────
❌ Не исправляет скомпрометированный ключ (это временное решение)
❌ Не гарантирует победу в гонке (быстрый бот может выиграть)
❌ Не предотвращает замену делегации (если ключ известен боту)
❌ Не работает с claim-контрактами, проверяющими msg.sender


УСТРАНЕНИЕ ПРОБЛЕМ
───────────────────────────────────────────────────────────────────

"Delegation dropped — renewing..."
  → Нормально, если бот тоже пытается своп.
  → Демон будет восстанавливать делегацию автоматически.

"ERR renewAndSweep reverted" — как реально диагностировать
  → Не гадай по расходу газа — просимулируй точный вызов через
    read-only eth_call (без приватного ключа, без реальной
    транзакции) и прочитай selector ошибки:

      client.CallContract(ctx, ethereum.CallMsg{
          From: sponsorAddr, To: &srcAddr, Data: sweepAllCalldata,
      }, nil)

    Если ошибка реализует интерфейс rpc.DataError — вызови
    .ErrorData(), чтобы получить сырые байты revert. Первые 4 байта
    — selector кастомной ошибки; сравни с error-объявлениями в
    своём контракте (DelegationNotActive(), TransferFailed()),
    чтобы точно понять что сработало.

  → Возможные причины (примерно по частоте):
    1. DelegationNotActive() срабатывает, хотя делегация выглядит
       правильной ончейн — см. отдельный раздел ниже, это был
       реальный баг в контракте, а не проблема делегации
    2. Недостаточно газа у спонсора на конкретной сети
    3. Чужая SetCodeAuthorization попала в тот же блок с меньшим
       nonce (настоящая гонка условий — редко, но возможно)
    4. Необычная логика в контракте токена (blacklist, pause)

⚠️ DelegationNotActive() при явно правильной делегации
───────────────────────────────────────────────────────────────────
Это больно ударило во время разработки, стоит задокументировать
точно — легко повторить эту же ошибку, если снова трогать
_verifyDelegation().

Ловушка: во время исполнения кода через EIP-7702 делегацию,
address(this) внутри контракта-делегата НЕ указывает на адрес
самого этого контракта — он указывает на ДЕЛЕГИРУЮЩИЙ EOA
(скомпрометированный кошелёк, например anaxine.eth). Потому что
байткод делегата исполняется "от лица" EOA: address(this),
msg.sender во внешнем фрейме, storage и balance — всё это
привязано к EOA, а не к контракту, чей код одолжен.

Две сломанные версии, которые реально были задеплоены по очереди:

  1. Проверка msg.sender — это кошелёк-спонсор (тот, кто отправил
     транзакцию), обычный EOA без делегации. Ревертит всегда.

  2. Проверка address(this) — кажется логичным ("проверь свой
     собственный designator"), но address(this) в момент вызова
     равен ДЕЛЕГИРУЮЩЕМУ адресу, а не этому контракту. Проверка
     превращается в "равен ли адрес получателя делегации адресу
     самого делегирующего" — что никогда не может быть истиной,
     потому что designator всегда указывает на реальный адрес
     RescuerV2, а не на сам EOA. Ревертит всегда, без явного
     сигнала, что баг именно в проверке, а не в делегации.

Исправление: сохранить собственный адрес контракта в immutable,
установленный в конструкторе — единственном месте, где
address(this) гарантированно означает адрес именно этого
контракта, потому что конструктор всегда исполняется в
собственном контексте деплоя, никогда через делегацию:

  address public immutable self;

  constructor(address _destination) {
      destination = _destination;
      self = address(this);   // корректно сохранено здесь, при деплое
  }

  function _verifyDelegation() internal view {
      bytes memory code = address(this).code;  // верно: код ДЕЛЕГИРУЮЩЕГО
      // ... парсинг 0xef0100 + 20-байтный адрес ...
      if (delegatedTo != self) revert DelegationNotActive();  // сравнение с immutable
  }

Если будешь ещё раз редеплоить или менять контракт — держи в
голове: читать designator нужно из address(this) (делегирующий,
в момент вызова), а СРАВНИВАТЬ его нужно с self (сам контракт,
зафиксированный при деплое) — никогда не сравнивай address(this)
с самим собой, это всегда даст неверный результат.

"RPC chain ID mismatch"
  → RPC-эндпоинт указывает на другую сеть.
  → Проверь RPC_URL_<NETWORK> соответствует нужной сети.

"startup sanity check failed" / "CRITICAL: RescuerV2 at ... has
destination=... baked in, but .env DESTINATION_ADDRESS=..."
  → Демон отказался стартовать на этой сети, потому что immutable
    destination задеплоенного контракта не совпадает с .env.
  → Обычно значит: либо DESTINATION_ADDRESS поменяли в .env уже
    после деплоя (без передеплоя контракта), либо RESCUER_<NETWORK>
    указывает на старый/неправильный адрес.
  → Поправь .env под реально задеплоенный контракт, либо
    задеплой RescuerV2 заново с правильным destination.
  → Не обходи эту проверку — она специально не даёт средствам
    молча уйти не туда.

"invalid address for RESCUER_BASE" (или похожее при старте)
  → Адрес в .env не является корректным 0x-адресом (40 hex-символов).
  → Скорее всего опечатка. Демон намеренно падает здесь громко —
    иначе опечатка могла бы тихо превратиться в другой,
    валидно выглядящий, но неверный адрес.

"insufficient funds for gas"
  → Баланс спонсора очень низкий.
  → Добавь 0.01+ ETH и перезапусти.

"transaction type is not supported" (zkSync)
  → Секвенсер zkSync пока не принимает EIP-7702 Type-4 транзакции.
  → Это ограничение сети. Демон будет безобидно повторять попытки
    делегации, но своп на zkSync не заработает до появления
    поддержки на уровне сети.

"symbol() call failed"
  → Токен нестандартный или контракт невалиден.
  → Демон покажет адрес вместо символа, sweep продолжится.


БЕЗОПАСНОСТЬ
───────────────────────────────────────────────────────────────────

Что защищено на уровне кода (после аудита и правок):

  ✓ executeAndSweep() ограничен только спонсором (onlySponsor)
    — без этого кто угодно мог бы заставить делегированный EOA
    вызвать approve() или любую другую функцию на любом токене,
    в обход immutable destination
  ✓ Конструктор RescuerV2 теперь принимает ДВА аргумента:
    (destination, sponsor) — деплой с одним аргументом просто
    не пройдёт, это ожидаемое, безопасное поведение
  ✓ Проверка при старте: chainId у RPC совпадает с ожидаемым,
    и on-chain destination() контракта совпадает с .env —
    иначе демон откажется запускаться на этой сети
  ✓ Адреса из .env строго валидируются — опечатка (неверная
    длина, лишний символ) вызывает явную ошибку при старте,
    а не тихую подстановку другого адреса
  ✓ Лимит повторов (3 попытки) для зависшего/сломанного токена
    теперь действует централизованно внутри renewAndSweep() —
    не обходится ни через живые события, ни через periodicCheck
  ✓ Верхний предел газа применён ко ВСЕМ трём типам транзакций,
    оплачиваемых спонсором (sweep, обновление делегации, sweepEth),
    не только к sweep — иначе скачок сети мог привести к
    непредсказуемо дорогой транзакции
  ✓ Успешный receipt сам по себе не считается доказательством
    свипа — демон дополнительно проверяет реальный баланс,
    т.к. при проигранной nonce-гонке транзакция могла молча
    исполниться через чужую (атакующего) делегацию
  ✓ PermitSweeper.permitAndTransfer() больше не принимает `to`
    как параметр — подпись permit подтверждает только (owner,
    spender, value, deadline), но НЕ получателя. Раньше это
    позволяло скопировать чужую подпись из mempool и вывести
    средства на свой адрес первым. Теперь получатель жёстко
    зашит как immutable owner

Рекомендации:

  ⚠ Никогда не коммитьте .env в GitHub
  ⚠ Храните приватные ключи в безопасности
  ⚠ Используйте аппаратный кошелёк для спонсора, если возможно
  ⚠ Мониторьте логи на необычную активность
  ⚠ Планируйте миграцию на новый кошелёк со свежим ключом
  ⚠ После любого редеплоя RescuerV2/PermitSweeper внимательно
    читайте итоговую сводку деплой-скрипта — она теперь явно
    и отдельно предупреждает, если критичный фикс (onlySponsor)
    не задеплоился хоть где-то, а не просто пишет общий "успех"

  ✓ После успешного rescue не используйте старый адрес для нового value
  ✓ Поделитесь опытом в сообществе (помогает другим в похожей ситуации)


ДЕПЛОЙ RescuerV2 НА НОВУЮ СЕТЬ
───────────────────────────────────────────────────────────────────
Если нужен RescuerV2 на сети, где его ещё нет:

Способ 1: TypeScript скрипт (если есть Node.js)
  npm install
  npx tsx scripts/deployRescuerV2.ts
  (Скрипт задеплоит и заполнит .env автоматически)
  (Конструктор требует ДВА аргумента: destination и sponsor —
   скрипт передаёт их сам, вручную вмешиваться не нужно)

Способ 2: Remix IDE (вручную)
  1. Откой remix.ethereum.org
  2. Скопируй contracts/RescuerV2.sol
  3. Компилируй Solidity 0.8.23
  4. Деплой на нужной сети
  5. Передай DESTINATION_ADDRESS как аргумент конструктора
  6. Скопируй адрес в .env как RESCUER_<NETWORK>

Способ 3: Hardhat/Foundry (если установлены)
  forge create contracts/RescuerV2.sol:RescuerV2 \
    --constructor-args "0x<DESTINATION>" \
    --rpc-url <RPC_URL> \
    --private-key <SPONSOR_KEY>


ТЕСТИРОВАНИЕ ДО ПРОДАКШЕНА
───────────────────────────────────────────────────────────────────
Сухой прогон (симуляция без отправки):

  1. Отредактируй .env:
     DRY_RUN=true

  2. Запусти:
     ./guard-daemon.exe

  3. Логи покажут что БЫЛ БЫ выполнено без реальных транзакций.

После проверки:
  DRY_RUN=false
  ./guard-daemon.exe


ЖИЗНЕННЫЙ ЦИКЛ СПАСЁННЫХ ТОКЕНОВ
───────────────────────────────────────────────────────────────────
1. ✅ Токены приходят на SOURCE
2. ✅ Демон их спасает → DESTINATION
3. ✅ Делегация остаётся активной (будет обновляться периодически)

Следующий шаг:
  → Создай новый кошелёк с НОВЫМ ключом
    (Ledger, Trezor, MetaMask с криптостойкой генерацией)
  → Для будущих airdrops используй НОВЫЙ адрес в регистрациях
  → Старый адрес оставь только для древних airdrop-клеймов


ПРОИЗВОДИТЕЛЬНОСТЬ
───────────────────────────────────────────────────────────────────
  Стартап:                <100 мс
  Задержка обнаружения:   <500 мс (WebSocket)
  Сборка транзакции:      ~100 мс
  Подтверждение:          12-60 сек (зависит от сети)
  Память:                 ~50 МБ
  CPU (в покое):          <1%


СВЯЗАННЫЕ ПРОЕКТЫ
───────────────────────────────────────────────────────────────────
  eip7702-rescue (TypeScript)
    https://github.com/Serge693/eip7702-rescue
    Оригинальный инструмент (медленнее, но та же идея)

  Flashbots Protect
    https://protect.flashbots.net/
    Приватный RPC (комплементарно, не решает race condition)

  MEV-Share
    https://docs.flashbots.net/flashbots-mev-share/
    Bundle-упаковка (полезно для известных клеймов)


ОБРАЗОВАТЕЛЬНЫЙ КОНТЕКСТ
───────────────────────────────────────────────────────────────────
Этот инструмент решает РЕАЛЬНУЮ, ЗАДОКУМЕНТИРОВАННУЮ проблему:
слабая энтропия в старых генераторах кошельков (MyEtherWallet
2015-2017) привела к предсказуемым ключам, которые боты могут
перебрать и мониторить.

Дополнительно:
  When Revoking Isn't Enough: Building a Tool to Outrun the Bot
  https://medium.com/@skartanenkov/...

  EIP-7702 Specification
  https://eips.ethereum.org/EIPS/eip-7702


═══════════════════════════════════════════════════════════════════
⚠️  ВАЖНО:
Этот инструмент — временное решение для спасения токенов из
скомпрометированного кошелька. Он не исправляет корневую причину
(компрометацию ключа).

Планируй миграцию на новый кошелёк со СВЕЖИМ ключом как можно
скорее.
═══════════════════════════════════════════════════════════════════
